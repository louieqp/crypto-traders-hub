import sqlite3

def is_valid_pair():
    return

def open_trade(user_id: int, ) -> list:
    """
    Description.

    :param user_id: 
    """
    connection = sqlite3.connect("database/database.db")
    cursor = connection.cursor()

    return 

def close_trade(user_id: int, ) -> list:
    """
    Description.

    :param user_id: 
    """
    connection = sqlite3.connect("database/database.db")
    cursor = connection.cursor()

    return 

def get_trades(user_id: int, status: str) -> list:
    """
    Description.

    :param user_id: 
    """
    connection = sqlite3.connect("database/database.db")
    cursor = connection.cursor()

    return 

def get_all_trades() -> list:
    """
    Description.

    :param user_id: 
    """
    connection = sqlite3.connect("database/database.db")
    cursor = connection.cursor()

    return 